const INTEGER = 'INTEGER'
const PLUS = 'PLUS'
const MINUS = 'MINUS'
const MUL = 'MUL'
const DIV = 'DIV'
const LPAREN = 'LPAREN'
const RPAREN = 'RPAREN'
const EOF = 'EOF'

module.exports = {
  INTEGER,
  PLUS,
  MINUS,
  MUL,
  DIV,
  LPAREN,
  RPAREN,
  EOF,
}
